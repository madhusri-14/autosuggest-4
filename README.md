# autosuggest-4

verifying my git set-up 
auto check

flow of pushing
**UI**
add(plus button)
commit with message
push / sync

**COMMANDS :**
git add -A
git commit -m "cmd prompt for npm"
git push




